<%= render 'books/list', user: @user %>

<% @books.each do |book| %>
<div>
  <p><%= image_tag @user.get_profile_image(100,100) %></p>
  <p>Title : 
  <%= link_to book_path(book.id) do %>
  <%= book.title %>
  <% end %>
  </p>
  <p>Body：<%= book.body %></p>
  <p><%= book.user.name %></p>
</div>
<% end %>
